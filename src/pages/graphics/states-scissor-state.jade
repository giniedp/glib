//- title: States - ScissorState
extends ./../_layouts/default.jade
block script
  script(type='text/javascript').
    var device = new Glib.Graphics.Device({canvas: 'canvas'});

    var width = device.canvas.width;
    var height = device.canvas.height;
    var halfWidth  = width/2;
    var halfHeight = height/2;

    var BLUE  = 0xFFFF0000;
    var GREEN = 0xFF00FF00;
    var RED   = 0xFF0000FF;
    var WHITE = 0xFFFFFFFF;

    device.scissorState = { x: 0, y: 0, width: halfWidth, height: halfHeight, enable: true };
    device.clear(RED);

    device.scissorState = { x: halfWidth };
    device.clear(GREEN);

    device.scissorState = { y: halfHeight };
    device.clear(BLUE);

    device.scissorState = { x: 0 };
    device.clear(WHITE);

    device.scissorState = {enable: false};

