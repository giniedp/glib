extends ./../../_layouts/example.jade
block content
   #mouse-container
block script
  script(type='text/javascript').
    var mouse = new Glib.Input.Mouse({
      el: document.getElementById('canvas')
    });
    mouse.on('changed', function(){
      document.getElementById('mouse-container').innerHTML = JSON.stringify(mouse.state);
    });
